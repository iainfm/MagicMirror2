<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

    <SwitchPreference
        android:defaultValue="false"
        android:key="autorun_switch"
        android:summary="@string/pref_description_autorun"
        android:title="@string/pref_title_autorun" />

    <Preference
        android:title="Weather"
        android:summary="Powered by darksky.net; icons by dribbble.com/heeyeun; API by John Hiott"/>
    <Preference
        android:title="News"
        android:summary="Supplied by BBC, Sky News, and Traffic Scotland"/>
    <Preference
        android:title="App"
        android:summary="Written by Iain McLaren, with much help from the internet"/>

</PreferenceScreen>
